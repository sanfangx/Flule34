org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# AGP 9 默认强制依赖使用唯一 package/namespace；Google Cronet 的
# cronet-api 与 cronet-shared 有意共用 org.chromium.net。迁移期恢复
# AGP 8.13 的校验行为，保留两者以避免运行时缺类。
android.uniquePackageNames=false
# 工程与 Pub 缓存位于不同磁盘，关闭 Kotlin 增量缓存以避免跨盘路径错误。
kotlin.incremental=false
# Kotlin 2.3.20 的默认 daemon 在本机 JDK 25 环境反复连接失败；进程内策略由官方支持，CI 的 JDK 17 同样适用。
kotlin.compiler.execution.strategy=in-process
