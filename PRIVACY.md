# Flule34 隐私说明

最后更新：2026-08-08

Flule34 是 Rule34Video 的 Android 客户端。App 会按用户操作向 `rule34video.com` 请求页面、JSON、图片和视频，并受网站自身隐私政策与服务条款约束。

## App 本地保存的数据

- 年龄确认状态与 App 设置；
- 加密安全存储中的登录 Cookie 和稳定用户 ID；
- 设备级观看进度和按账号隔离的搜索历史；
- 与账号无关的本地分类库、设备级下载记录和公共下载文件；
- 图片缓存和播放器所需的临时网络数据。
- 用户手动译文和自动学习的标题、标签、分类译文；自动学习译文会长期保存在本机，直到用户在翻译设置中明确清除；
- 最近 7 天的本地诊断日志；日志按天滚动、限制单日和总占用空间，并在写入前自动脱敏。

账号、密码和登录 Cookie 保存在 Android 安全存储中，用于自动恢复登录；主动退出登录时会一并删除。未登录状态不能使用网站收藏、历史和订阅，但可以使用设备级下载与本地分类库。

Android 系统云备份和设备迁移会排除 Flule34 的数据库、设置、下载文件与安全状态，避免账号数据被系统备份复制到其他设备。

## 网络传输

登录、收藏、订阅、搜索、播放和下载会直接与 Rule34Video 通信。打开资料编辑、密码或邮箱管理时，App 会调用系统浏览器访问对应官方 HTTPS 页面。App 自身不集成广告 SDK、分析 SDK 或第三方崩溃上报 SDK。本地诊断日志不会自动上传，只有用户主动导出并通过系统分享面板选择目标时才会离开设备。

用户可以在翻译设置中配置多个第三方翻译服务，并分别勾选标题、标签和分类的自动翻译。自动翻译默认关闭；只有用户开启对应目标，或在标题、标签、分类编辑框中主动点击翻译服务时，当前单项文本才会发送给用户配置的服务。艺术家、上传者、model 和频道不会进入翻译请求。服务返回的译文会保存在本机中文翻译库中，不会因服务排序变化、普通缓存清理或内置词表更新而删除；用户手动译文优先级最高。

每个 AI 翻译服务首次成功使用前，App 可能使用同一条当前待翻译文本依次尝试关闭思考、降低思考或不传思考参数，以确定该服务兼容的请求方式；一旦某种方式成功，接口类型、基址和模型的组合及所选策略会缓存在本机，后续请求不会重复探测。该缓存不包含译文原文、API Key 或服务返回内容，修改接口类型、基址或模型后会自动失效。

中文翻译库可由用户主动导出为 JSON 文件。导出文件包含内置译文、第三方服务返回的 API 译文和用户手动译文，但不包含 API Key、登录信息、Cookie 或翻译服务配置；文件只有在用户通过系统分享面板选择目标后才会离开设备。导入时 App 会读取用户主动选择的 JSON 文件，只导入 API 与用户译文层，文件中的内置译文会被忽略。

## 诊断信息

诊断报告和应用日志只在设备本地生成。诊断报告包含版本、构建模式、Android 型号/版本、CPU ABI、数据库版本、下载记录数量和设置摘要；日志记录异常类别、脱敏错误信息和必要的运行状态，不记录请求正文、搜索词、视频标题、标签、译文、Cookie、API Key、密码、邮箱、用户 ID 值、Android ID、序列号或广告标识符。日志最多保留 7 天，并设置单日 2 MB、总计 10 MB 的空间上限。只有用户主动导出和发送时才会离开设备。

## 删除数据

“隐私与数据”可删除当前账号的搜索历史、图片缓存和全部本地诊断日志，并可导出最近 7 天日志；这些操作不影响设备级下载任务或公共下载文件。中文翻译库可以按来源删除 API 译文或撤销用户覆盖，不会删除内置词表；批量删除 API 译文需要确认。网站服务器上的资料、收藏和历史需通过网站提供的账号功能管理。下载管理可删除未被外部改名或修改的公共文件；卸载 App 会删除 App 私有数据，但公共 `Download/Flule34` 中的文件需要用户自行删除。

## 年龄与法律

Flule34 仅面向达到所在地法定成年年龄且可合法访问相关内容的用户。年龄确认是产品提示，不替代用户遵守所在地法律的责任。
